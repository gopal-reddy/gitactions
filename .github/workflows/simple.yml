name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo Hello World
        run: eco "Hello World"
      - name: echo working
        run: echo "working"

